# Repo tracks only lib/ + assets/ + CI configs. Native platform folders are
# regenerated by CI via `flutter create` (see README), so they're ignored.
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Dart/Flutter build artifacts & caches
.dart_tool/
.packages
build/
*.lock.bak
pubspec.lock

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
